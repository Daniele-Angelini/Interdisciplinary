import fs from 'node:fs/promises';import path from 'node:path';const root=process.cwd(),dist=path.join(root,'dist');await fs.rm(dist,{recursive:true,force:true});await fs.mkdir(dist,{recursive:true});for(const f of ['index.html','styles.css','app.js'])await fs.copyFile(path.join(root,f),path.join(dist,f));await fs.cp(path.join(root,'data'),path.join(dist,'data'),{recursive:true});await fs.cp(path.join(root,'papers'),path.join(dist,'papers'),{recursive:true});console.log('Build completata in dist/');
